public void testCodota() { 
  BufferedReader reader = //...here is the magic
}
from index import Config

print(Config())

INDEX_DEBUG=on
INDEX_ENV=pro

{
  "dev": {
    "debug": true,
  },
  "pro": {
    "debug": false,
    "port": 34567,
    "host": "0.0.0.0"
  }
}
